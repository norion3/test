const canvas=document.getElementById("game"),ctx=canvas.getContext("2d");function resize(){canvas.width=innerWidth;canvas.height=innerHeight}resize();addEventListener("resize",resize);const player={x:150,y:0,w:100,h:20},ball={x:100,y:-50,r:20,speed:6};let score=0;function move(x){player.x=Math.max(0,Math.min(x-player.w/2,canvas.width-player.w))}canvas.addEventListener("touchmove",e=>{e.preventDefault();move(e.touches[0].clientX)});canvas.addEventListener("touchstart",e=>move(e.touches[0].clientX));function resetBall(){ball.x=Math.random()*(canvas.width-40)+20;ball.y=-20}function update(){player.y=canvas.height-60;ball.y+=ball.speed;if(ball.y+ball.r>player.y&&ball.x>player.x&&ball.x<player.x+player.w){score++;resetBall()}if(ball.y>canvas.height)resetBall()}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle="white";ctx.font="30px sans-serif";ctx.fillText("Score : "+score,20,50);ctx.fillStyle="cyan";ctx.fillRect(player.x,player.y,player.w,player.h);ctx.beginPath();ctx.arc(ball.x,ball.y,ball.r,0,Math.PI*2);ctx.fillStyle="yellow";ctx.fill()}resetBall();(function loop(){update();draw();requestAnimationFrame(loop)})();